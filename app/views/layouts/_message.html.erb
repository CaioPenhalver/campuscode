<% if !flash.empty? %>
  <% flash.each do |key, value| %>
    <% if key == "success" || key == "danger" %>
      <div class="alert alert-<%= key %>" role="alert">
        <%= value %>
      </div>
    <% end %>
  <% end %>
<% end %>
